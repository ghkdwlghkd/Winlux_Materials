<nav class="navbar navbar-expand navbar-light bg-white topbar mb-4 static-top shadow">
	<ul class="navbar-nav ml-auto">
		<li class="nav-item dropdown no-arrow">
			<a
				class="nav-link dropdown-toggle"
				href="#"
				id="userDropdown"
				role="button"
				data-toggle="dropdown"
				aria-haspopup="true"
				aria-expanded="false"
			>
				<span class="mr-2 d-none d-lg-inline text-gray-600 small"></span>
				<!-- 로그인시 이름 넣으세요. 비로그인시 비워두면 됩니다. -->
				<!-- <img class="img-profile rounded-circle" src="/img/undraw_profile.svg" /> -->
			</a>
			<!-- Dropdown - User Information -->
			<!-- <div id='loginout' class="dropdown-menu dropdown-menu-right shadow animated--grow-in" aria-labelledby="userDropdown">
			</div> -->
			
		</li>
	</ul>
</nav>

<script>
	
	 $(function() {
      const cookies = document.cookie.split(';').map((el) => el.split('='));
      const accessCookie = cookies.filter((e) => e[0].trim() == 'access');
      const access = (accessCookie.length > 0) ? accessCookie[0][1] : "0";
	
      const idCookie = cookies.filter((e) => e[0].trim() == 'id');
      const id = (idCookie.length > 0) ? idCookie[0][1] : "0";
		
		
		console.log(access);
		console.log(id);
		 		//   if (access == "10") {
				// 	  var template = `<a class="dropdown-item" href="/logout"><i class="fas fa-user fa-sm fa-fw mr-2 text-gray-400"></i>로그아웃</a>
				// 	  				  `;
				// 	$("#loginout").append(template);
				//   }	else   if (access == "0") {
				// 	  var template = `<a class="dropdown-item" href="/login"><i class="fas fa-user fa-sm fa-fw mr-2 text-gray-400"></i>로그인</a>
				// 	  				  `;
				// 	$("#loginout").append(template);
				//   }
		 
	 });

	   
</script>